#include<iostream.h>
#include<conio.h>
void main()
{
 clrscr();
 char a[20];
 int caps=0,lower=0;
 cout<<"Enter a word ";
 cin>>a;
 for(int i=0;a[i]!='\0';i++)
   {
    if(a[i]>='A'&&a[i]<='Z')
      caps++;
    if(a[i]>='a'&&a[i]<='z')
      lower++;
   }
 cout<<"\nNo. of capital alphabets are "<<caps;
 cout<<"\nNo. of alphabets in lower case are "<<lower;
 getch();
}
