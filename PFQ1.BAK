#include<iostream.h>
#include<conio.h>
void main()
{
 clrscr();
 int a[10],i,x;
 cout<<"Emter 10 number ";
 for(i=0;i<10;i++)
   cin>>a[i];
 x=a[0];
 for(i=1;i<10;i++)
   if(a[i]<x)
    x=a[i];
 cout<<"the smallest no. is "<<x;
 getch();
}
